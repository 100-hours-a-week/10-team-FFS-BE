spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ktb_final_10_ffs?serverTimezone=Asia/Seoul&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: create-drop

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/kloset}

# 테스트용 더미 값
jwt:
  secret: ZekytZNiGwsikVrTSys4bKDbz1MB3XaMdgtTHX693S7
  access-token-expiration: 43200000
  refresh-token-expiration: 604800000

cloud:
  aws:
    s3:
      bucket: test-bucket

kakao:
  client-id: 316561850df4fcec4d8c8f0aaecefc90
  client-secret: 7ykeQGcKUfTvRL39moTullIJYvAmam23
  redirect-uri: http://localhost:3000/oauth/kakao/callback
  token-uri: https://kauth.kakao.com/oauth/token
  jwks-uri: https://kauth.kakao.com/.well-known/jwks.json
  issuer: https://kauth.kakao.com

cors:
  allowed-origins:
    - http://localhost:3000
  allowed-methods:
    - GET
    - POST
    - PUT
    - DELETE
    - PATCH
    - OPTIONS
  allowed-headers:
    - '*'
  allow-credentials: true
  max-age: 3600

ai:
  api:
    base-url: http://localhost:8080

internal:
  api-key: ci-test-internal-key

rate-limit:
  ai-api-daily-limit: 1000000
  general-api-minute-limit: 1000000
